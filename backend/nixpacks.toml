[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip3 install -r requirements.txt"]

[phases.build]
cmds = ["python3 manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn reddit_clone.wsgi --log-file -"
